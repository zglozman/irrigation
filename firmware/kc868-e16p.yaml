substitutions:
  device_name: irrigation-controller
  # Hard on-device cap: a valve can never stay open longer than this, even if
  # the cloud scheduler crashes after sending ON or connectivity drops before
  # the OFF arrives. The cloud's scheduled OFF is the normal path.
  max_valve_runtime: 60min

esphome:
  name: ${device_name}
  friendly_name: Irrigation Controller

# ESP-IDF framework is required for MQTT mutual-TLS (client certificates)
# used to authenticate to AWS IoT Core.
esp32:
  board: esp32dev
  framework:
    type: esp-idf

logger:

# Local Home Assistant / native API — kept for local debugging.
# reboot_timeout must be 0s: with no HA client ever connecting, the default
# watchdog would reboot the board every ~15 minutes.
api:
  encryption:
    key: !secret api_encryption_key
  reboot_timeout: 0s

# Two OTA paths:
#  - esphome: push from the LAN via the esphome CLI (bench/dev updates)
#  - http_request: device pulls firmware from a URL (cloud OTA) — paired with
#    the update: component below, which checks an S3-hosted manifest so the
#    board can be updated remotely without LAN access.
ota:
  - platform: esphome
    password: !secret ota_password
  - platform: http_request

http_request:

update:
  - platform: http_request
    name: Firmware Update
    source: !secret ota_manifest_url

# Board is PoE/Ethernet-first. If your PoE injector/switch isn't in yet,
# comment this out and uncomment the wifi: block below instead.
ethernet:
  type: LAN8720
  mdc_pin: GPIO23
  mdio_pin: GPIO18
  clk:
    mode: CLK_OUT
    pin: 17
  phy_addr: 0

# wifi:
#   ssid: !secret wifi_ssid
#   password: !secret wifi_password
#   ap:
#     ssid: "${device_name} Fallback Hotspot"
#     password: !secret ota_password

# AWS IoT Core over mutual TLS. The cloud scheduler publishes ON/OFF to
# ${device_name}/switch/relay_N/command; ESPHome publishes state and
# availability (birth/last-will) automatically.
mqtt:
  broker: !secret aws_iot_endpoint
  port: 8883
  discovery: false
  topic_prefix: ${device_name}
  # Must match the client-id allowed by the IoT policy — ESPHome's default
  # appends a MAC suffix, which IoT Core would reject on Connect.
  client_id: ${device_name}
  certificate_authority: !secret aws_root_ca
  client_certificate: !secret aws_iot_device_cert
  client_certificate_key: !secret aws_iot_device_key

# Web dashboard on the device's IP — lets you flip relays from a browser
# with zero cloud/backend involved, good for bench testing.
web_server:
  port: 80

i2c:
  sda: GPIO16
  scl: GPIO15
  scan: true
  id: bus_a

pcf8574:
  - id: pcf_out_1_8
    address: 0x21
  - id: pcf_out_9_16
    address: 0x25
  - id: pcf_in_1_8
    address: 0x22
  - id: pcf_in_9_16
    address: 0x24

switch:
  - platform: gpio
    name: "Relay 1"
    id: relay_1
    pin: {pcf8574: pcf_out_1_8, number: 0, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_1
  - platform: gpio
    name: "Relay 2"
    id: relay_2
    pin: {pcf8574: pcf_out_1_8, number: 1, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_2
  - platform: gpio
    name: "Relay 3"
    id: relay_3
    pin: {pcf8574: pcf_out_1_8, number: 2, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_3
  - platform: gpio
    name: "Relay 4"
    id: relay_4
    pin: {pcf8574: pcf_out_1_8, number: 3, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_4
  - platform: gpio
    name: "Relay 5"
    id: relay_5
    pin: {pcf8574: pcf_out_1_8, number: 4, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_5
  - platform: gpio
    name: "Relay 6"
    id: relay_6
    pin: {pcf8574: pcf_out_1_8, number: 5, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_6
  - platform: gpio
    name: "Relay 7"
    id: relay_7
    pin: {pcf8574: pcf_out_1_8, number: 6, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_7
  - platform: gpio
    name: "Relay 8"
    id: relay_8
    pin: {pcf8574: pcf_out_1_8, number: 7, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_8
  - platform: gpio
    name: "Relay 9"
    id: relay_9
    pin: {pcf8574: pcf_out_9_16, number: 0, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_9
  - platform: gpio
    name: "Relay 10"
    id: relay_10
    pin: {pcf8574: pcf_out_9_16, number: 1, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_10
  - platform: gpio
    name: "Relay 11"
    id: relay_11
    pin: {pcf8574: pcf_out_9_16, number: 2, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_11
  - platform: gpio
    name: "Relay 12"
    id: relay_12
    pin: {pcf8574: pcf_out_9_16, number: 3, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_12
  - platform: gpio
    name: "Relay 13"
    id: relay_13
    pin: {pcf8574: pcf_out_9_16, number: 4, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_13
  - platform: gpio
    name: "Relay 14"
    id: relay_14
    pin: {pcf8574: pcf_out_9_16, number: 5, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_14
  - platform: gpio
    name: "Relay 15"
    id: relay_15
    pin: {pcf8574: pcf_out_9_16, number: 6, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_15
  - platform: gpio
    name: "Relay 16"
    id: relay_16
    pin: {pcf8574: pcf_out_9_16, number: 7, mode: OUTPUT, inverted: true}
    on_turn_on:
      - delay: ${max_valve_runtime}
      - switch.turn_off: relay_16

binary_sensor:
  - platform: gpio
    name: "Input 1"
    pin: {pcf8574: pcf_in_1_8, number: 0, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 2"
    pin: {pcf8574: pcf_in_1_8, number: 1, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 3"
    pin: {pcf8574: pcf_in_1_8, number: 2, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 4"
    pin: {pcf8574: pcf_in_1_8, number: 3, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 5"
    pin: {pcf8574: pcf_in_1_8, number: 4, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 6"
    pin: {pcf8574: pcf_in_1_8, number: 5, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 7"
    pin: {pcf8574: pcf_in_1_8, number: 6, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 8"
    pin: {pcf8574: pcf_in_1_8, number: 7, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 9"
    pin: {pcf8574: pcf_in_9_16, number: 0, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 10"
    pin: {pcf8574: pcf_in_9_16, number: 1, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 11"
    pin: {pcf8574: pcf_in_9_16, number: 2, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 12"
    pin: {pcf8574: pcf_in_9_16, number: 3, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 13"
    pin: {pcf8574: pcf_in_9_16, number: 4, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 14"
    pin: {pcf8574: pcf_in_9_16, number: 5, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 15"
    pin: {pcf8574: pcf_in_9_16, number: 6, mode: INPUT, inverted: true}
  - platform: gpio
    name: "Input 16"
    pin: {pcf8574: pcf_in_9_16, number: 7, mode: INPUT, inverted: true}
